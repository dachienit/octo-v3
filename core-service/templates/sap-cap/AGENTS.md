This is an SAP CAP workspace.

Use the vendored SAP CAP skills under `skills/sap-cap-capire` for CAP modeling, services, handlers, Fiori annotations, deployment, and troubleshooting.

Prefer project structures and examples aligned with SAP Cloud Application Programming Model conventions. Preserve existing CAP files such as `db/schema.cds`, `srv/*.cds`, `srv/*.js`, `srv/*.ts`, `mta.yaml`, and `xs-security.json` when making changes.
